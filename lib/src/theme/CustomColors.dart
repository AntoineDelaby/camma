import 'package:flutter/material.dart';

class CustomColors {
  static const primary = Color.fromRGBO(91, 231, 123, 1.0);
  static const primary_dark = Color.fromRGBO(25, 140, 55, 1.0);
  static const secondary = Color.fromRGBO(55, 120, 25, 1.0);
}